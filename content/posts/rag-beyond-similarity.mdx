export const metadata = {
  title: "RAG, 더 잘 찾는 것만으로 충분할까?",
  description: "GraphRAG의 구축부터 비용까지. 기본 검색이 놓치는 근거를 어떻게 연결하고, 방대한 문서를 어떻게 처리하며, 어떤 질문에 추가 비용을 쓸지 살펴봅니다.",
  category: "AI Research",
  publishedAt: "2026-09-10",
};

export const sections = [
  { id: "question", title: "잘 찾았는데, 왜 답하지 못할까?" },
  { id: "limits", title: "문서가 많아지면 검색은 한계에 도달할까?" },
  { id: "graphrag", title: "GraphRAG는 무엇을 바꾸는가?" },
  { id: "construction", title: "그래프는 누가, 어떻게 만드는가?" },
  { id: "scale", title: "문서 10만 개를 처리한다면?" },
  { id: "cost", title: "추가 비용을 지불할 만한 질문인가?" },
  { id: "alternatives", title: "같은 문제를 푸는 다른 설계들" },
  { id: "evaluation", title: "내 RAG에 무엇을 추가할까?" },
  { id: "references", title: "참고 자료" },
];

<aside className="article-note" aria-label="이 글의 조사 범위">
  <span className="article-eyebrow">Research note · 2026.09.10</span>
  <p>GraphRAG의 구축부터 비용과 적용 기준까지 다룹니다. 2022~2025년에 처음 공개된 RAG 연구와 공식 문서를 중심으로 조사했습니다. 예시와 운영 설계는 필자의 제안이며, 직접 수행한 성능 실험이나 현재 서비스의 견적은 포함하지 않았습니다.</p>
</aside>

<h2 id="question">잘 찾았는데, 왜 답하지 못할까?</h2>

사내 문서를 검색하는 챗봇을 만들었다고 하자. “결제 API의 타임아웃은 몇 초인가?”라는 질문에는 꽤 잘 답한다. 설정값이 적힌 문서를 찾으면 된다. 그런데 다음 질문들은 어떨까?

“결제 장애에 어떤 서비스들이 연결되어 있었을까?”

“지난해 장애 기록 전반에서 반복된 원인은 무엇일까?”

첫 질문은 특정 사실을 찾는 일이다. 두 번째는 여러 문서의 대상을 연결해야 하고, 세 번째는 기록 전체를 살펴 공통 주제를 종합해야 한다. 모두 검색창에 입력하는 질문이지만 필요한 근거의 범위는 다르다.

벡터 기반 의미 검색, sparse retrieval, RRF, reranking은 후보를 찾고 순서를 개선하는 중요한 도구다. 오늘은 그 다음을 살펴보고 싶다. **근거가 여러 문서에 흩어져 있다면, 무엇을 미리 연결해두고 어떤 순서로 읽어야 할까?**

Microsoft의 GraphRAG는 관계 그래프와 커뮤니티 요약으로 이런 질문에 접근한다. 다만 구조를 추가하면 추출·갱신 비용도 생긴다. 동작 원리에서 출발해, 실제로 그 구조를 만드는 일과 비용을 지불할 이유까지 따라가 보자. [GraphRAG 논문](https://arxiv.org/abs/2404.16130)

<h2 id="limits">문서가 많아지면 검색은 한계에 도달할까?</h2>

문서 수가 특정 값을 넘으면 임베딩 검색이 무너지는 보편적인 경계는 없다. 수십억 벡터를 다루는 유사도 검색 연구도 있다. 하지만 검색 속도를 확보하는 것과 질문에 필요한 근거를 빠짐없이 찾는 것은 별개다. [Billion-scale similarity search with GPUs](https://arxiv.org/abs/1702.08734)

이 글에서는 실패를 세 가지로 나누어 보겠다.

<h3>정답이 후보에 들어오지 않는다</h3>

문서가 늘면 비슷한 제품의 설명, 과거 버전, 복사된 매뉴얼도 늘 수 있다. 정답과 닮은 다른 문서가 상위 결과를 차지하는 상황이다. 이런 경우에는 제품·날짜 필터, 중복 제거, 키워드와 의미 검색의 결합을 먼저 검토하겠다. 데이터의 양만으로 그래프가 필요한지 판단하기는 어렵다.

<h3>필요한 연결 고리가 질문과 닮지 않았다</h3>

일반적인 리랭커는 검색기가 가져온 후보를 다시 평가한다. 후보 100개에 필요한 문서가 없다면 순서를 바꿔도 그 문서를 복구할 수 없다. 후보를 늘리면 평가할 작업량도 늘어난다. [Retrieve & Re-Rank](https://www.sbert.net/examples/sentence_transformer/applications/retrieve_rerank/README.html)

가령 결제 장애 보고서는 인증 서비스를 언급하고, 인증 서비스의 별도 변경 기록에는 연결 풀 설정이 적혀 있다고 하자. 두 번째 문서에는 ‘결제 장애’라는 말이 없을 수도 있다. 첫 문서를 읽고 인증 서비스로 다시 검색하거나, 이미 연결된 관계를 활용할 이유가 여기서 생긴다.

<h3>상위 문서가 전체를 대표하지 못한다</h3>

“지난해 장애의 공통 원인”을 묻는데 검색 결과가 특정 사건의 문서로 가득하다면, 개별 결과는 관련성이 높아도 다른 유형의 장애를 놓칠 수 있다. 이때는 순위뿐 아니라 전체 자료의 포괄성이 문제다. 질문에 필요한 정보를 미리 묶어 요약하거나, 여러 범위를 나누어 조사하는 접근을 검토할 수 있다.

따라서 내 선택 기준은 문서 수보다 **정답 문서를 놓쳤는지, 연결 고리를 놓쳤는지, 전체 경향을 놓쳤는지**다. GraphRAG는 이 중 관계와 종합이 필요한 질문을 살펴보는 출발점이다.

<h2 id="graphrag">GraphRAG는 무엇을 바꾸는가?</h2>

그래프를 활용하는 RAG에는 여러 구현이 있다. 여기서는 **2024년에 공개된 Microsoft GraphRAG**를 기준으로 살펴본다.

기본 아이디어는 문서를 읽어 엔티티와 관계를 추출하고, 연결이 밀접한 노드들을 커뮤니티로 묶은 뒤, 그 내용을 요약해두는 것이다. 엔티티는 사람이나 조직뿐 아니라 서비스, 기술, 사건처럼 문서에서 식별할 대상이다. 커뮤니티는 그래프의 연결 구조를 바탕으로 얻는 묶음이다. [GraphRAG 논문](https://arxiv.org/abs/2404.16130)

가상의 장애 기록을 관계로 표현하면 다음과 같다. 화살표에는 단순한 연결 여부뿐 아니라 관계의 의미가 담겨야 한다.

```text
결제 서비스 ── 의존한다 ──→ 인증 서비스
주문 서비스 ── 의존한다 ──→ 인증 서비스
장애 A      ── 영향을 줬다 → 결제 서비스
장애 B      ── 영향을 줬다 → 주문 서비스
```

이 구조를 보면 두 서비스가 공유하는 의존성을 조사할 수 있다. 다만 공통 의존성이 있다는 것만으로 인증 서비스가 두 장애의 원인이라고 결론 내릴 수는 없다. 원인 관계는 각 보고서의 근거로 따로 확인해야 한다.

<h3>Local Search: 특정 대상의 주변을 읽는다</h3>

“결제 서비스와 연결된 시스템은?”처럼 대상이 뚜렷한 질문에서는 관련 엔티티를 출발점으로 삼는다. 연결된 엔티티와 관계, 관련 원문 조각 등을 모아 컨텍스트를 구성한다. 그래프와 원문을 함께 이용하는 방식이다. [Local Search](https://microsoft.github.io/graphrag/query/local_search/)

<h3>Global Search: 문서 집합 전체의 주제를 읽는다</h3>

“장애 기록 전반에서 반복되는 문제는?”이라는 질문은 출발점 하나를 고르기 어렵다. Global Search는 지정한 커뮤니티 계층의 보고서들을 바탕으로 중간 답변을 만들고, 중요한 내용을 모아 최종 답변을 생성한다. 여러 묶음을 처리한 뒤 결과를 합치는 map-reduce 방식이다. [Global Search](https://microsoft.github.io/graphrag/query/global_search/)

여기서 그래프의 역할은 질문마다 경로를 따라가는 일에만 있지 않다. **데이터 전체를 어떤 단위로 나누어 읽을지 준비하는 역할**도 한다. 커뮤니티를 요약하는 과정에서 여러 기록의 공통 주제가 검색 가능한 형태로 만들어진다.

이 설명을 설계에 적용할 때는 두 가지를 구분하고 싶다. 전체 주제를 설명하는 일과 정확한 빈도를 집계하는 일이다. “가장 흔한 원인”의 순위와 건수가 필요하다면, 요약문에서 추정하기보다 사건을 중복 없이 구조화하고 집계하는 단계를 별도로 두겠다. 요약에 덜 드러난 소수의 중요한 사건도 원문에서 확인할 수 있어야 한다.

<h2 id="construction">그래프는 누가, 어떻게 만드는가?</h2>

사람이 모든 문서를 읽고 선을 그리지는 않는다. 표준 GraphRAG에서는 LLM이 문서 조각마다 엔티티와 관계를 추출하고, 프로그램이 작은 그래프들을 병합한다. 커뮤니티는 Hierarchical Leiden 알고리즘으로 구성하고, 보고서는 LLM으로 작성한다. [공식 색인 파이프라인](https://microsoft.github.io/graphrag/index/default_dataflow/)

사람이 맡을 중요한 일은 추출의 목적과 품질 기준을 정하는 것이다. 장애 분석이라면 서비스·장애·배포·팀을 대상으로 삼고, 의존·영향·담당 관계를 우선 다루는 식이다. 다음은 내가 이 용도에 맞춰 설계한다면 사용할 기준이다. GraphRAG의 기본 출력 스키마와는 구분된다.

<div className="article-table-wrap" role="region" aria-label="장애 분석 그래프의 설계 예시" tabIndex={0}>
  <table>
    <thead><tr><th scope="col">정할 것</th><th scope="col">예시</th></tr></thead>
    <tbody>
      <tr><th scope="row">대상 종류</th><td>서비스, 장애, 배포, 팀</td></tr>
      <tr><th scope="row">관계 종류</th><td>의존한다, 영향을 줬다, 담당한다</td></tr>
      <tr><th scope="row">식별 기준</th><td>서비스 ID, 장애 티켓 번호, 저장소 주소</td></tr>
      <tr><th scope="row">근거와 시간</th><td>문서 ID·버전, 원문 문장, 사건 시점</td></tr>
    </tbody>
  </table>
</div>

<h3>한 문장에서 관계 후보를 만든다</h3>

“결제 서비스는 인증 서비스에 토큰 검증을 요청한다. 이번 장애의 원인은 아직 조사 중이다.”라는 문장을 읽었다고 하자. 추출 결과를 다음처럼 저장할 수 있다.

```json
{
  "source": "결제 서비스",
  "relation": "의존한다",
  "target": "인증 서비스",
  "evidence": "결제 서비스는 인증 서비스에 토큰 검증을 요청한다.",
  "document_id": "incident-042",
  "document_version": 3
}
```

여기서 의존 관계는 추출할 수 있지만, 인증 서비스가 장애를 유발했다고 기록하면 안 된다. 출력이 올바른 JSON인지 확인하는 일과 의미가 원문에 의해 뒷받침되는지 확인하는 일도 다르다. 원문 구간의 존재와 출력 형식은 코드로 검증하고, 관계의 타당성은 표본 검토와 도메인별 평가로 확인하겠다. LLM이 붙인 자신감 점수만으로 사실을 승인하지는 않겠다.

<h3>문서마다 다르게 부르는 대상을 통합한다</h3>

더 어려운 일은 문서 A의 ‘인증 서비스’, 문서 B의 ‘Auth Service’, 문서 C의 ‘auth-api’가 같은 대상인지 판단하는 것이다. Microsoft의 기본 파이프라인은 제목과 타입이 같은 엔티티를 병합한다. 복잡한 별칭이나 동명이인까지 해결하려면 추가 설계가 필요하다. [기본 엔티티 병합](https://microsoft.github.io/graphrag/index/default_dataflow/)

나는 고유 ID와 공식 서비스 목록을 먼저 사용하고, 별칭 사전으로 알려진 이름을 묶겠다. 남은 경우에만 타입·이름·문맥으로 후보를 좁혀 LLM이나 사람이 검토하도록 하겠다. 모든 대상 쌍을 비교하는 방식은 규모가 커질수록 부담이 커진다. 확신이 부족한 대상을 일단 별개로 남겨두는 규칙도 필요하다.

서비스 목록이나 의존 관계가 이미 운영 시스템에 있다면 그것을 출발점으로 삼을 수 있다. 문서 추출 결과는 기존 대상에 연결하고, 각 관계에는 근거를 여러 개 보존하겠다. 한 원문이 삭제돼도 다른 문서의 근거가 남아 있을 수 있기 때문이다.

<h2 id="scale">문서 10만 개를 처리한다면?</h2>

방대한 문서를 한 번에 모델에 넣는 대신, 작은 작업을 반복 실행하는 색인 시스템이 필요하다. 아래는 대량 처리를 위한 설계 제안이다. 공식 라이브러리를 설치하면 이 분산 운영 전체가 자동으로 갖춰진다는 뜻은 아니다.

```text
문서 수집·텍스트 추출
  → 중복 제거·문서 버전 기록
  → 원문 위치를 보존하며 조각 생성
  → 작업 큐 → 여러 추출 워커
  → 출력 형식·근거 검증
  → 같은 대상 식별·관계 병합
  → 그래프 저장 → 커뮤니티 탐지 → 보고서 생성
```

PDF라면 표와 본문의 읽기 순서부터 확인해야 한다. 관계가 잘못 추출된 원인이 LLM이 아니라 깨진 텍스트일 수도 있다. 조각을 만들 때도 제목·절·표처럼 의미 있는 경계를 고려하고 원문으로 돌아갈 위치를 남기겠다.

가령 문서 10만 개를 평균 10개 조각으로 나누면 **100만 개의 추출 작업**이 생긴다. 조각당 한 번 호출한다고 가정해도 100만 번이다. 이는 규모를 설명하기 위한 계산이며, 실제 호출 수는 묶음 처리와 재추출 설정 등에 따라 달라진다. 문서 수만으로 비용을 추정하지 말고, 표본에서 입력·출력 토큰과 처리 시간을 측정해야 한다.

문서별 추출은 병렬화하기 좋지만, 처리량 제한을 넘겨 워커만 늘리면 재시도가 쌓일 수 있다. 다음 항목을 함께 설계하겠다.

- **작업량 제어:** 요청·토큰 제한에 맞춰 동시 실행 수를 조절하고 실패 유형별로 재시도한다.
- **캐시와 재시작:** 완료된 결과를 저장하고 실패한 작업부터 재개한다. 문서 조각의 해시와 모델·프롬프트·설정 버전을 함께 관리한다.
- **단계 분리:** 개별 추출과 문서 간 병합·커뮤니티 계산을 분리한다. 표본 작업의 처리 속도를 전체 파이프라인의 속도로 착각하지 않는다.

GraphRAG 자체에도 동일한 프롬프트와 설정의 요청 결과를 재사용하는 LLM 캐시가 있다. 다만 캐시가 모든 버전 관리와 변경 영향 추적을 대신하는 것은 아니다. [색인 아키텍처와 캐시](https://microsoft.github.io/graphrag/index/architecture/)

<h3>수정한 문서 하나가 그래프 전체에 영향을 줄 수 있다</h3>

새 문서의 추출만 끝냈다고 갱신이 완료되지는 않는다. 기존 관계가 부정되거나 서비스 이름이 바뀌면 관련 노드, 관계 설명, 커뮤니티 보고서도 검토해야 한다. 연결 구조가 달라지면 커뮤니티의 경계 자체가 바뀔 수도 있다.

내가 구현한다면 원문과 파생 결과 사이의 의존성을 기록하고, 변경된 근거가 영향을 주는 결과를 다시 생성하겠다. 최신 관계를 반영하는 작업과 주기적인 커뮤니티 재계산을 분리할 수 있지만, 그 사이 어떤 버전의 요약을 제공할지 정해야 한다. 삭제·수정된 문서의 내용이 예전 보고서에 남아 검색되는 경우도 평가에 포함하겠다.

<h2 id="cost">추가 비용을 지불할 만한 질문인가?</h2>

기본 RAG와 비교할 때는 최초 색인, 갱신, 질문당 비용을 나누어야 한다. 다음 표는 임베딩과 리랭킹을 사용하는 검색 RAG와 표준 GraphRAG의 작업을 비교한 것이다. 모든 GraphRAG 구현이 같은 비용을 갖는 것은 아니다.

<div className="article-table-wrap" role="region" aria-label="기본 RAG와 GraphRAG의 비용 발생 단계" tabIndex={0}>
  <table>
    <thead><tr><th scope="col">단계</th><th scope="col">임베딩·리랭킹 RAG</th><th scope="col">표준 GraphRAG</th></tr></thead>
    <tbody>
      <tr><th scope="row">초기 색인</th><td>문서 분할, 임베딩, 검색 인덱스</td><td>엔티티·관계 추출과 병합, 커뮤니티 탐지·요약, 임베딩</td></tr>
      <tr><th scope="row">질문 처리</th><td>후보 검색, 리랭킹, 답변 생성</td><td>Local은 관련 그래프·원문 수집, Global은 보고서별 중간 답변 종합</td></tr>
      <tr><th scope="row">문서 갱신</th><td>변경된 조각과 검색 인덱스 갱신</td><td>변경된 근거와 관계, 파생 요약의 영향 범위 처리</td></tr>
      <tr><th scope="row">운영 관리</th><td>검색 품질, 버전, 인덱스 운영</td><td>추가로 대상 식별, 관계 품질, 요약의 일관성 관리</td></tr>
    </tbody>
  </table>
</div>

표준 GraphRAG는 색인 단계에서도 생성 모델을 사용한다. Microsoft는 그래프 추출이 색인 비용의 약 75%를 차지한다고 추정한다. 이는 해당 구현에 대한 추정이며 모든 시스템에 적용되는 비율은 아니다. [공식 색인 방식 비교](https://microsoft.github.io/graphrag/index/methods/)

차이가 크게 벌어진 공개 사례도 있다. 2024년 LazyGraphRAG 발표는 해당 평가 설정의 색인 비용이 벡터 RAG와 같고, 전체 GraphRAG의 0.1%였다고 보고했다. 역산하면 약 1,000배의 차이다. 하지만 AP 기사 5,590건과 합성 질문 100개를 사용한 특정 평가의 보고이므로, 현재 프로젝트의 견적 배수로 사용해서는 안 된다. [LazyGraphRAG의 비용과 평가 조건](https://www.microsoft.com/en-us/research/blog/lazygraphrag-setting-a-new-standard-for-quality-and-cost/)

질문당 비용은 탐색 범위에 따라 달라진다. 작은 영역을 모아 한 번 답하는 Local과 많은 보고서에서 중간 답변을 만드는 Global을 같은 비용으로 묶을 수 없다. 시간도 토큰 처리량, 동시 실행, 반복 탐색의 단계 수에 따라 달라진다. 나는 다음 식의 항목을 각각 측정하겠다.

```text
기간 전체 비용
= 최초 색인 비용
+ 기간 중 갱신 비용
+ 질문 수 × 평균 질문 처리 비용
+ 저장·운영 비용
```

질문이 많아지면 초기 비용을 나눠 부담할 수 있다. 그러나 질문당 비용까지 더 높다면 사용량만으로 경제성이 생기지는 않는다. 더 좋은 답변이 사람의 조사 시간을 얼마나 줄이는지도 봐야 한다.

<h3>산업 이름보다 실제 질문에서 출발한다</h3>

아래는 구조를 바탕으로 한 적용 가설이다. 각 영역에서 우수성이 검증되었다는 의미는 아니다.

<div className="article-table-wrap" role="region" aria-label="도메인과 질문에 따른 적용 후보" tabIndex={0}>
  <table>
    <thead><tr><th scope="col">영역·질문</th><th scope="col">검토할 접근</th><th scope="col">선택 이유</th></tr></thead>
    <tbody>
      <tr><th scope="row">운영: 서비스 변경의 영향 범위는?</th><td>의존 관계 그래프 + 원문 검색</td><td>서비스·배포·장애의 연결을 확인</td></tr>
      <tr><th scope="row">공급망: 거래처들이 공유하는 공급업체는?</th><td>기업 식별과 관계 탐색</td><td>문서마다 다른 이름을 같은 대상으로 연결</td></tr>
      <tr><th scope="row">연구: 결과가 갈리는 실험 조건은?</th><td>관계·계층 검색 + 원문 검증</td><td>연구 대상·방법·결과를 여러 문서에서 비교</td></tr>
      <tr><th scope="row">조직 지식: 반복된 실패 요인은?</th><td>커뮤니티·계층 요약</td><td>여러 프로젝트의 공통 주제를 종합</td></tr>
      <tr><th scope="row">개발 문서: 이 API의 설정값은?</th><td>기본 검색·리랭킹</td><td>버전이 맞는 개별 근거를 찾는 것이 핵심</td></tr>
      <tr><th scope="row">운영 통계: 원인별 장애 건수는?</th><td>정형 데이터와 집계</td><td>중복 제거와 정확한 계산이 필요</td></tr>
    </tbody>
  </table>
</div>

자주 바뀌는 자료에서 단순 사실을 빨리 찾는 일이 대부분이라면 선행 그래프 구축의 이점이 작을 수 있다. 반대로 식별 가능한 대상과 관계가 풍부하고, 연결을 조사하는 질문을 반복한다면 구축 결과를 재사용할 이유가 커진다. 기존 시스템에 신뢰할 수 있는 관계가 있다면 문서에서 모두 재추출할 필요도 없다.

<h2 id="alternatives">같은 문제를 푸는 다른 설계들</h2>

관계 그래프는 선택지 중 하나다. 무엇을 저장할지, 어떻게 탐색할지, 언제 분석 비용을 쓸지에 따라 다른 접근을 함께 검토할 수 있다.

<h3 id="raptor">여러 수준의 맥락이 필요하다면: RAPTOR</h3>

2024년의 RAPTOR는 텍스트 조각을 임베딩하고 군집화해 요약한 뒤, 그 요약을 다시 묶어 여러 수준의 계층을 만든다. 문서의 목차를 그대로 옮기는 방식은 아니며 soft clustering에서는 조각 하나가 여러 군집에 포함될 수 있다. 주요 실험에서는 루트부터 내려가기보다 모든 계층의 노드를 함께 검색하는 collapsed tree를 사용한다. [RAPTOR 논문](https://arxiv.org/html/2401.18059v1)

검색 대상에 원문과 넓은 요약을 함께 두므로 질문에 맞는 설명의 크기를 고를 수 있다. 설계 방향을 묻는 질문에는 요약이, 재시도 횟수를 묻는 질문에는 원문이 유용할 수 있다. 다만 요약에서 빠진 조건과 예외를 확인하도록 원문 연결을 보존하겠다.

<h3 id="hipporag">연결된 근거를 찾고 싶다면: HippoRAG 2</h3>

2025년의 HippoRAG 2는 추출한 구절뿐 아니라 원문 passage도 그래프 노드로 포함한다. 임베딩으로 관련 트리플을 찾고 LLM으로 걸러낸 뒤, 구절과 passage의 신호를 Personalized PageRank에 반영해 원문을 고른다. 질문에서 출발한 가중치를 그래프 연결을 따라 분배하며 연관된 근거를 찾는 방식이다. [HippoRAG 2 논문](https://arxiv.org/html/2502.14802v1)

‘기억’이라는 이름은 모델 바깥의 검색 구조로 지식을 반영하는 관점에서 읽어야 한다. 사람의 기억이 구현되었다거나, 높은 그래프 점수가 논리적 증명이라는 뜻은 아니다. 이 방식에서도 잘못 합친 서비스 이름이 엉뚱한 문서로 탐색을 이끄는지 확인하고 싶다.

<h3 id="adaptive">찾은 사실에 따라 검색이 달라진다면: 반복 탐색</h3>

IRCoT는 중간 추론을 다음 검색에 활용하고 검색 결과로 추론을 이어간다. 2022년 프리프린트, 2023년 ACL 논문으로 공개되었다. 첫 질문만으로 필요한 근거를 모두 정하기 어려운 상황을 다룬다. [IRCoT 논문](https://arxiv.org/abs/2212.10509)

```text
결제 장애 보고서 검색
  → 인증 서비스 의존성 확인
  → 해당 시점의 인증 서비스 변경 이력 검색
  → 원문이 뒷받침하는 사실을 모아 답변
```

Microsoft GraphRAG의 DRIFT는 관련 커뮤니티 보고서로 초기 답변과 후속 질문을 만든 뒤 Local Search로 세부 정보를 탐색한다. 구조와 반복 탐색을 함께 사용하는 예다. [DRIFT Search](https://microsoft.github.io/graphrag/query/drift_search/)

반복 호출에는 종료 기준이 필요하다. 호출 예산뿐 아니라 새로운 근거가 추가되는지도 기록하고, 근거가 부족한 상태로 종료하는 경우도 허용하겠다. 검색과 자기 평가를 학습으로 제어하는 Self-RAG도 관련 연구다. 다만 reflection token을 사용하도록 모델을 학습하는 방법이므로 일반 모델에 확인 프롬프트를 추가하는 것과 구현 요구가 다르다. [Self-RAG 논문](https://arxiv.org/abs/2310.11511)

<h3 id="lazy">사전 분석 비용을 줄이고 싶다면: LazyGraphRAG</h3>

2024년 소개된 LazyGraphRAG는 NLP로 명사구와 동시 출현 관계를 추출해 개념 그래프를 만들고, 사전 LLM 요약을 생략한다. LLM 분석을 질의 시점으로 미루고, 관련성 평가 예산으로 탐색 작업량을 조절한다. 함께 등장한 개념의 연결은 ‘의존한다’나 ‘유발했다’ 같은 의미 관계와 다르다. [LazyGraphRAG 공식 소개](https://www.microsoft.com/en-us/research/blog/lazygraphrag-setting-a-new-standard-for-quality-and-cost/)

FastGraphRAG도 NLP와 동시 출현을 활용하지만 커뮤니티 보고서는 색인 단계에 LLM으로 생성한다. LazyGraphRAG와 같은 방식으로 혼동해서는 안 된다. 공식 FastGraphRAG 문서는 기본 NLP 설정이 주로 영어에 적합하다고 설명하므로, 한국어 문서라면 별도의 추출 품질 검증이 필요하다. [FastGraphRAG 문서](https://microsoft.github.io/graphrag/index/methods/)

<h2 id="evaluation">내 RAG에 무엇을 추가할까?</h2>

원 GraphRAG 논문의 주요 평가는 약 100만~170만 토큰 규모의 두 문서 집합에서 전체 맥락을 종합하는 질문을 다뤘다. 그 결과만으로 모든 규모와 질문에서 임베딩·리랭킹보다 우수하다고 결론 내릴 수는 없다. [평가 범위와 한계](https://arxiv.org/html/2404.16130v1)

내가 시작한다면 대표 문서 수백 개와 실제 질문 수십 개로 작은 실험을 만들겠다. 다음 순서로 판단할 수 있다.

1. **실패를 분류한다.** 단일 사실, 관계 연결, 전체 종합 중 어디에서 부족한지 본다.
2. **비교 기준을 맞춘다.** 같은 원문·생성 모델을 사용하고, 컨텍스트와 비용 예산을 기록한다. 기본 검색에는 적절한 필터와 리랭킹을 적용한다.
3. **필요한 변화만 추가한다.** 후보 누락이면 검색을, 연결 누락이면 반복 탐색·그래프를, 전체 포괄성 부족이면 계층·커뮤니티 요약을 시험한다.
4. **근거와 운영을 함께 평가한다.** 필요한 문서와 연결 고리를 찾았는지, 주요 주제를 빠뜨리지 않았는지, 인용이 주장을 지지하는지 본다. 색인·갱신·질의 비용과 응답 지연도 함께 측정한다.
5. **수정과 삭제를 시험한다.** 원문이 바뀐 뒤 과거 관계나 요약이 계속 답변에 사용되는지 확인한다.

복잡한 질문이 개선되어도 원래 잘 풀던 쉬운 질문이 나빠질 수 있다. 기본 검색을 유지하고 관계 분석이나 전체 종합이 필요한 질문에만 추가 단계를 사용하는 구성도 검토하겠다. 이때 질문을 잘못 분류했을 때의 실패까지 함께 봐야 한다.

이번에 살펴본 RAG의 변화는 검색 대상이 문장에서 관계와 요약으로 넓어지고, 검색 시점이 답변을 구성하는 과정까지 확장된다는 데 있다. 내게 중요한 기준은 그 구조가 새로운가보다 **실패했던 질문에 필요한 근거를 더 잘 제공하는가**다. 그리고 더 풍부한 연결을 제시할수록, 독자가 그 연결의 원문 근거를 확인할 수 있어야 한다.

<h2 id="references">참고 자료</h2>

아래 원문과 공식 문서를 2026년 9월 10일 확인했다. 연도는 최초 공개 시점을 기준으로 표기했다. 논문별 데이터와 평가 조건이 다르므로 수치를 하나의 성능 순위로 합치지 않았다.

- [From Local to Global: A Graph RAG Approach to Query-Focused Summarization](https://arxiv.org/abs/2404.16130) — 2024. Microsoft GraphRAG의 출발점.
- [GraphRAG Local Search](https://microsoft.github.io/graphrag/query/local_search/), [Global Search](https://microsoft.github.io/graphrag/query/global_search/), [DRIFT Search](https://microsoft.github.io/graphrag/query/drift_search/) — 공식 검색 방법 문서.
- [RAPTOR: Recursive Abstractive Processing for Tree-Organized Retrieval](https://arxiv.org/html/2401.18059v1) — 2024. 요약 계층 구성과 검색 방법.
- [From RAG to Memory: Non-Parametric Continual Learning for Large Language Models](https://arxiv.org/html/2502.14802v1) — 2025. HippoRAG 2의 그래프와 검색 절차.
- [Interleaving Retrieval with Chain-of-Thought Reasoning for Knowledge-Intensive Multi-Step Questions](https://arxiv.org/abs/2212.10509) — 2022 프리프린트, ACL 2023. IRCoT.
- [Self-RAG: Learning to Retrieve, Generate, and Critique through Self-Reflection](https://arxiv.org/abs/2310.11511) — 2023 프리프린트, ICLR 2024. 검색과 자기 평가를 위한 학습.
- [LazyGraphRAG: Setting a new standard for quality and cost](https://www.microsoft.com/en-us/research/blog/lazygraphrag-setting-a-new-standard-for-quality-and-cost/) — 2024. 질의 시점으로 미룬 LLM 분석과 평가 조건.
- [GraphRAG Indexing Dataflow](https://microsoft.github.io/graphrag/index/default_dataflow/), [Architecture](https://microsoft.github.io/graphrag/index/architecture/), [Methods](https://microsoft.github.io/graphrag/index/methods/) — 추출·병합·커뮤니티 탐지와 캐시, Standard·Fast 방식의 차이.
- [Retrieve & Re-Rank](https://www.sbert.net/examples/sentence_transformer/applications/retrieve_rerank/README.html) — Sentence Transformers의 후보 검색과 재평가 구조.
- [Billion-scale similarity search with GPUs](https://arxiv.org/abs/1702.08734) — 2017. 대규모 벡터 검색 연구. 의미적 답변 품질의 보장을 뜻하지는 않는다.
